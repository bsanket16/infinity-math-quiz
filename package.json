{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "dev": "concurrently \"cd proj_backend && yarn dev\" \"cd proj_frontend && yarn start\" ",
    "start": "cd proj_backend && npm install && node app.js"
  },
  "author": "bsankt",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "google-auth-library": "^6.0.6",
    "node-fetch": "^2.6.0"
  }
}
